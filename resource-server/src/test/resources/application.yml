spring:
  application:
    name: api
  profiles:
    group:
      test: default_mvc_test, no_security_mvc_test, test
---
spring:
  config:
    activate:
      on-profile: test
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh

